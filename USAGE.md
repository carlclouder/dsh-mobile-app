# DSH Mobile 功能使用指南

> 本文档覆盖 App 所有功能的详细操作说明，适合初次使用的用户逐节阅读。

---

## 1. 环境要求

| 项 | 要求 |
|---|---|
| 手机 | Android 8.0（API 26）及以上 |
| 电脑 | 运行 DSH（`dsh web --host 127.0.0.1 --port 3080 --trusted-host <域名>`） |
| 组网 | 手机与电脑同一 Tailscale 账号，手机 VPN 保持开启 |

---

## 2. 安装与首次配置

### 2.1 获取 APK

从 [Releases](../../releases) 下载 `DSH-Mobile-release-v<版本>.apk`（约 2.5MB）。

### 2.2 安装

传到手机后点击安装。首次安装需允许"安装未知来源应用"。

> 同签名升级直接覆盖装（`adb install -r` 或直接点击新 APK）。debug 和 release 签名不同，混装必须先卸载旧包。

### 2.3 首次启动必做 3 件事

| # | 操作 | 不做的后果 |
|---|---|---|
| 1 | 允许通知权限 | 收不到任何提醒（回合完成/审批/提问） |
| 2 | 设置 → 服务器地址 → 填你的域名 → 保存 | App 不知道连哪台电脑，显示「不可达」 |
| 3 | 设置 → 电池优化 → 去系统设置 → 设为"不受限" | 安卓杀后台，息屏后断连、收不到通知 |

完成后首页顶部应显示 **「已连接」**。

---

## 3. 会话列表（首页）

### 3.1 三态分组

| 分组 | 颜色 | 含义 |
|---|---|---|
| 跑动中 | 绿点 | agent 正在执行任务（显示约运行时长） |
| 等你输入 | 琥珀点 | 上一轮已完成，等你发消息 |
| 空闲 | 灰点 | 超过 7 天未活动的旧会话 |

### 3.2 基本操作

| 操作 | 方法 |
|---|---|
| 进入会话 | 点击任意会话行 |
| 新建会话 | 右下角 **+** → 选择工作区 → 自动进入新会话 |
| 刷新列表 | 手指从中间往下拉（下拉刷新） |
| 切换工作区 | 顶部「工作区 ▾」→ 选择目标工作区 |
| 折叠工作区条 | 点击「工作区 ▾/▴」 |

### 3.3 会话操作（⋮ 菜单）

每个会话行尾有 **⋮** 按钮：

| 操作 | 说明 |
|---|---|
| 重命名 | 修改会话标题 |
| 分叉会话 | 从当前会话分出子会话，自动跳转 |
| 归档会话 | 从列表隐藏（不删除） |

### 3.4 排序规则

同组内按标题字母排序。空白会话（从未发过消息）和子代理会话自动隐藏。

---

## 4. 对话页

### 4.1 发送消息

底部长方形输入框 → 打字 → 点**发送**（或键盘回车）。

- 消息发出后立即显示在对话流中（乐观回显）
- agent 回复实时流式显示（逐字出现）
- agent 运行中顶条显示**「停止」**按钮，点击可中断

### 4.2 语音输入

输入框右侧 **🎤** 按钮：

1. 首次使用需授权麦克风
2. 点一下开始说话，说完自动停止并填入输入框
3. 不会自动发送，可编辑后再发

> 无谷歌语音服务的手机（多数国产机型）：App 自动降级到系统"识别活动"通道。如果仍不行，用输入法自带的麦克风。

### 4.3 模型选择

顶条 **`◆ provider/model`** → 底部弹出选择器：

- 上半区：按供应商分组的模型列表（点击即选中）
- 下半区：当前模型的思考等级（如 high/medium/low）

选择后立即生效，下一轮对话使用新模型。

### 4.4 排队消息与插话

agent **运行中**时发送的消息不会打断当前轮次，而是进入排队。

排队区显示在对话底部：**「排队中（N）」**，展开后每条消息有三个按钮：

| 按钮 | 功能 |
|---|---|
| ✎ 编辑 | 修改排队消息的文字 |
| 🗑 删除 | 删除该条排队消息 |
| ⚡插话 | 把该条消息**立即注入**当前正在运行的轮次（agent 在下一步边界会读到它） |

> 插话后该条从排队列表消失，agent 尽快消费。插话按钮在 agent 空闲时置灰（此时直接发新消息即可开始新一轮）。

### 4.5 消息展示

| 元素 | 说明 |
|---|---|
| 用户消息 | 右侧蓝色气泡 |
| AI 回复 | 左侧灰色气泡，支持 Markdown（标题/加粗/代码块/列表/图片） |
| 思考过程 | 灰色单行「💭 思考过程 · 点按展开」，默认折叠 |
| 工具调用 | 单行「🔧 工具名 · 参数摘要」，点按展开完整参数 |
| 工具结果 | 单行「🔧 工具结果 · 结果摘要」，点按展开完整输出 |
| 模型错误 | 红色气泡「⚠ 模型调用错误」+ 错误详情 |
| 时间戳 | 每条消息底部小字（MM-dd HH:mm） |
| 复制 | 长按消息文字 |

### 4.6 滚动行为

- **自动贴底**：新消息到达时，如果视口在底部附近，自动滚动跟随
- **拖动即停**：手指拖动列表时立即停止自动跟随（不再被拉回底部）
- **跳到底部**：离开底部后右下角出现「⬇ 跳到底部」悬浮按钮，点击恢复跟随
- **发消息恢复**：发送新消息时自动恢复贴底跟随

### 4.7 会话统计

对话底部信息栏显示当前会话的 token 用量：

```
轮·步 | LLM耗时·工具耗时 | 首token·tok/s | 缓存命中% | 输入token
```

---

## 5. 通知（核心功能）

### 5.1 三类通知

| 通知 | 触发时机 | 你能做什么 |
|---|---|---|
| 回合完成 | agent 跑完一轮等你输入 | 点按进对话页 |
| 审批请求 | agent 要执行敏感操作 | **通知上直接点「允许一次/拒绝」**，不用打开 App |
| Agent 提问 | agent 向你提问 | 点按进对话页答题 |

### 5.2 通知行为

- 前台服务常驻（顶栏有「DSH 监听中」通知，属正常，勿清退）
- 息屏/退后台都能收到通知
- 在别处已处理（如电脑上点了）→ 手机通知自动消失
- 应答发送失败 → 通知变"应答失败，请打开 App 重试"
- 三类通知可在设置中独立开关

---

## 6. 设置页

| 设置项 | 说明 |
|---|---|
| 服务器地址 | DSH 的访问地址（Tailscale 域名），保存后自动重连 |
| 测试连接 | 一键自检（TLS 握手 + HTTP 状态码） |
| 回合完成通知 | 开/关 |
| 审批请求通知 | 开/关 |
| Agent 提问通知 | 开/关 |
| 电池优化 | 跳转到系统设置，把 App 设为"不受限" |
| 外观 | 默认 / 护眼蓝 / 护眼绿 三套主题 |
| 关于 | 版本号、功能概要、技术栈、排障指引 |

---

## 7. 连接与断线恢复

### 7.1 正常连接

App 启动后自动连接服务器。首页顶部显示「已连接」= 正常。

### 7.2 断线

网络中断（WiFi 切换、Tunnel 断开）后：

- 顶部变为「重连中」或「不可达」
- 自动重连（最长间隔 15 秒）

### 7.3 自动恢复

以下情况**无需重启 App**，自动恢复：

- 电脑 DSH 重启后
- Tailscale 断开重连后
- 手机锁屏再解锁后
- 网络切换后

打开 App 的瞬间会立即尝试重连（不用等 15 秒）。

### 7.4 换服务器地址

设置 → 服务器地址 → 填新地址 → 保存。立即断开旧连接、连接新地址。

---

## 8. 已知限制

| 项 | 状态 |
|---|---|
| Markdown 表格 | 内容丢失（渲染库限制，暂不支持） |
| Mermaid 图 | 显示源码，不渲染图形 |
| 图片渲染 | 需图片 URL 真机可达（Tailscale 域名/IP） |
| 单用户 | 无配对/多账户机制 |
| 模型配置 | 以 PC 端 DSH 为权威源 |

---

## 9. 快速排障

| 症状 | 原因 | 解决 |
|---|---|---|
| 「不可达」 | Tailscale 断开 / 电脑关机 / DSH 未启动 | 检查手机 VPN → 电脑 Tailscale → 电脑 3080 端口 |
| 「不可达」但浏览器能打开 | 403：`dsh web` 没带 `--trusted-host` | 用正确参数重启 DSH |
| 会话列表空 | 还没有对话过的会话 | 在电脑上对话后下拉刷新 |
| 收不到通知 | 权限/电池优化/开关 | 设置 → 通知权限 → 电池 → 三类开关 |
| 语音不可用 | 无系统识别服务 | 安装讯飞语音+ 或用输入法麦克风 |
| 发消息无回复 | 模型额度用完/API 错误 | 查看对话内红色「⚠ 模型调用错误」气泡 |
| 插话后排队没消失 | 网络延迟 | 等待几秒，host 回显驱动；或下拉刷新 |

# DSH Mobile 使用说明

DSH Mobile 是 DeepSeek Harness（DSH）的安卓远程客户端：在手机上原生查看会话列表、
息屏接收回合完成/审批/提问通知、免开 APP 直接批审批、进入会话对话。

## 环境要求

| 项 | 要求 |
|---|---|
| 手机 | Android 8.0（API 26）及以上；任意 Android 8.0+ 设备 |
| PC | 运行 `dsh web`（监听 127.0.0.1:3080），并已 `tailscale serve` 发布到 `https://<PC-name>.tailnet.ts.net` |
| 组网 | 手机与 PC 同一 tailnet（手机装 Tailscale 并保持 VPN 开启） |

## 安装

APK 位置（发布版，默认安装包）：`<repo>\app\build\outputs\apk\release\DSH-Mobile-release-v<version>.apk`（约 2.5 MB，R8 混淆 + 正式签名）。
debug 版（模拟器 UI 验证用）在 `<repo>\app\build\outputs\apk\debug\DSH-Mobile-debug-v<version>.apk`。

方式一（USB 连手机后执行）：
> ⚠️ debug 版与 release 版**签名不同**，混用不可 `-r` 覆盖升级（报 `INSTALL_FAILED_UPDATE_INCOMPATIBLE`）——要从 debug 换 release（或反之）先卸载旧包再装。

```powershell
& "<repo>\.toolchain\android-sdk\platform-tools\adb.exe" install -r "<repo>\app\build\outputs\apk\release\DSH-Mobile-release-v<版本>.apk"
```

方式二：把 APK 传到手机（微信/网盘/局域网共享均可），文件管理器点开安装（需允许"安装未知来源应用"）。

## 首次启动

1. 打开 DSH Mobile → 系统弹"允许通知"→ **必须点允许**（否则所有提醒静默，前台服务照跑但你看不到任何通知）。
2. 首页顶部状态条显示"已连接"即成功；显示"不可达"请检查：
   - 手机 Tailscale VPN 是否在线；
   - PC 上 `dsh web` 是否在跑；
   - 设置页 → 测试连接。
3. 建议设置页 → 电池优化 → 去系统设置 → 把 DSH Mobile 设为"不允许"（不受限），保证整夜挂机息屏可达。

## 日常使用

### 会话列表（首页）
- 三段分组：**跑动中**（绿点，显示约运行时长）/ **等你输入**（琥珀点，回合已完成等你输入）/ **空闲**（灰点，7 天内无提问的靠后显示）
- 点任意会话 → 进入对话页（**原生 Compose 渲染**，非 WebView）
- 右下 **+** → 选择工作区新建会话
- 空白会话与子代理（subagent）会话默认隐藏

### 对话页（原生 Compose）
- **流式输出**：assistant 的文本/思考增量实时聚合，settled 后替换（对齐 PC 端实时体验）。
- **语音输入**：🎤 按钮调用系统语音识别，授权后自动开始；无识别服务的设备明确提示降级（用键盘麦克风输入）。
- 顶部：返回 / 会话标题 / ◆ 模型选择 / **停止**（向 PC 发取消请求）。
- **排队消息控制**：进入排队时每条消息带 **✎ 编辑 / 🗑 删除 / ⚡ 插话**。
- **会话统计栏**：轮·步 | LLM·工具耗时 | 首 token·tok/s | 缓存命中% | 输入 tok + 上下文已用%。
- **消息体验**：长按复制、时间戳、Markdown 渲染（标题/加粗/行内代码/代码块/列表/图片）。
- **自适应折叠**：思考过程、工具参数、工具结果过长（>400 字符）自动缩写 + 可展开；短内容全显、无折叠钮；思考结果（答案）始终完整展示。
- **跳到底部**：上翻列表时右下角出现「⬇ 跳到底部」按钮，点击一键回到最新消息并恢复自动滚动（底部时自动隐藏）。

### 通知（核心功能）
| 通知 | 含义 | 操作 |
|---|---|---|
| 回合完成，等待你的输入 | agent 跑完一轮等你说话 | 点按进对话页 |
| 审批请求：\<工具名\> | 会话请求执行敏感操作 | 通知上直接点 **允许一次 / 拒绝**（不用打开 APP） |
| Agent 提问 | agent 向你提问 | 点按进对话页答题 |

说明：
- 审批在别处已被处理（比如你在 PC 上点了）→ 手机通知自动消失，偶尔弹"该审批已在别处处理"。
- 应答发送失败（网络断）→ 通知变"应答失败，请打开 APP 重试"。
- 后台监听为常驻通知（"DSH 监听中"），属正常现象，勿清退；被系统杀掉会自动重启拉起。

### 设置
- **服务器地址**：默认 `https://<PC-name>.tailnet.ts.net`，可改（保存后自动断开重连新地址）
- **测试连接**：一键自检（TLS 握手 + HTTP 状态）
- **通知开关**：回合完成 / 审批请求 / Agent 提问，三类独立开关，即时生效

## 常见问题

**Q：收不到通知？**
① 设置 → 应用 → DSH Mobile → 通知权限是否开启；② Tailscale VPN 是否在线（状态条"不可达"即断）；③ 电池优化是否把应用杀了（去设置页点"去系统设置"加白）。

**Q：整夜挂机靠谱吗？**
服务类型用 specialUse（无 Android 15 的 6 小时限制）；但仍建议电池优化加白 + 保持 Tailscale 常驻。极端情况下服务被杀会自动重启（START_STICKY）并静默重建基线（冷启动不轰炸通知）。

**Q：换了服务器地址旧连接怎么办？**
保存新地址会立即断开旧事件流并整体迁移到新地址，不会出现两台服务器状态分裂。

**Q：每次装新版要卸载吗？**
**同签名**（如 release→release）用 `adb install -r` 直接覆盖升级，不用卸载；但**换了签名**（debug↔release 混装）必须先卸载旧包再装，否则报 `INSTALL_FAILED_UPDATE_INCOMPATIBLE`。

## 已知限制

- **App 内 Markdown 图片渲染未实测**：`![alt](url)` 走 Coil 加载，但图片 URL 必须**真机能访问**（用 Tailscale 域名/IP，如 `https://<PC-name>.tailnet.ts.net/ws/…` 或 `http://<PC-Tailscale-IP>:8123/…`）；用 `127.0.0.1` 会指向手机自己而加载不到。主机 WebUI 走 `/ws` 插件路由已确认可显示。
- 单机单用户设计（无配对/多账户）；模型配置、文件上传走 PC 端。
